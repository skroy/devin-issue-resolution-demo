# Server Configuration
PORT=3000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/finserv-platform

# Authentication
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRY=24h

# Rate Limiting (not yet used - values are hardcoded in rateLimit.js)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX=100
# AUTH_RATE_LIMIT_MAX=5

# Notifications (stub)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=notifications@finserv.co
# SMTP_PASS=
